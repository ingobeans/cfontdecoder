[package]
name = "cfontdecoder"
version = "0.4.0"
edition = "2024"

[dependencies]
image = { version = "0.25.10", default-features = false, features = [
    "png",
    "jpeg",
    "bmp",
    "webp",
    "ico",
] }
