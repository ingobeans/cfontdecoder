[package]
name = "cfontdecoder"
version = "0.3.1"
edition = "2024"

[dependencies]
image = { version = "0.25.10", default-features = false, features = [
    "png",
    "jpeg",
    "bmp",
    "webp",
    "ico",
] }
