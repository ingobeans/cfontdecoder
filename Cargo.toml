[package]
name = "cfontdecoder"
version = "0.5.0"
edition = "2024"

[dependencies]
image = { version = "0.25.10", default-features = false, features = [
    "png",
    "jpeg",
    "bmp",
    "webp",
    "ico",
] }
titanf = "3.0.0"
